# LibManager Pro - Environment Variables Template
# 
# Copy this file to `.env` and fill in your own values.
# IMPORTANT: Never commit the actual `.env` file to version control.

# ===== BACKEND (server/) =====

# MongoDB Connection String
# Local: mongodb://localhost:27017/library_db
# Atlas: mongodb+srv://username:password@cluster.mongodb.net/library_db?retryWrites=true&w=majority
MONGO_URI=mongodb://localhost:27017/library_db

# JWT Secret for token signing
# ⚠️ CRITICAL: Change this in production to a strong, random value
# Generate one: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Node.js Environment
# Values: development, staging, production
NODE_ENV=development

# Backend Server Port
PORT=5000

# CORS Origin (Frontend URL)
# Used for cross-origin requests from frontend
# Development: http://localhost:3000 or http://localhost:5173 (if using Vite)
# Production: https://yourdomain.com
CORS_ORIGIN=http://localhost:3000

# ===== FRONTEND (client/) =====

# Backend API URL
# Local: http://localhost:5000
# Production: https://api.yourdomain.com
VITE_API_URL=http://localhost:5000

# ===== OPTIONAL / FUTURE USE =====

# Email Service (for future notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Redis Cache (for future caching)
# REDIS_URL=redis://localhost:6379

# File Storage (for future book covers/PDFs)
# AWS_S3_BUCKET=your-bucket-name
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key

# ===== DOCKER COMPOSE OVERRIDE =====
# Uncomment and modify if running with Docker Compose

# MONGO_INITDB_ROOT_USERNAME=admin
# MONGO_INITDB_ROOT_PASSWORD=password123
# MONGO_INITDB_DATABASE=library_db
